name: focus_school_app
description: A focus timer and reward system for school.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # თუ მომავალში დაამატებ სხვა რამეს, აქ ჩაიწერება

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
